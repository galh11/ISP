#include <stdlib.h>

enum Months { JAN, FEB, MAR, /*...*/, DEC}

/*
    basically enum let's us define values to improve readability
*/

const type identifier = expression; //cannot change after initialization